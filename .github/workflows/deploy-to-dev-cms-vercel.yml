name: deploy-to-dev-cms-vercel
on:
  workflow_dispatch:
    inputs:
      Name:
        type: choice
        description: Whom to greet
        options: 
        - User1
        - User2
      Message:
        required: true
      DeployToCMS:
        type: boolean
        description: Deploy To CMS
        required: true
      DeployToVercel:
        type: boolean
        description: Deploy To Vercel
        required: true
      Environment:
        type: environment

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "${{ env.VAR1 }}- ${{ github.event.inputs.Message }} ${{ fromJSON('["", ""]')[github.event.inputs.DeployToCMS] }} ${{ github.event.inputs.Name }}"
