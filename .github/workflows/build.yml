name: Create SAP Build
on:
  push:
    tags:        
      - resideo_*.*
    branches: [ develop ]
jobs:
  Build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Create SAP Build
      run: echo "Good to deploy!"
